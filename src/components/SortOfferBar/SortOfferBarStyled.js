import styled from 'styled-components';

const SortOfferBarStyled = styled.div`
  display: flex;
  justify-content: space-between;
  margin: 10px 0;
  padding: 5px 0;

  h2 {
    margin :0;
  }

  select {
    padding: 5px;
    border-radius: 4px;
    border: 1px solid #dfdfdf;
  }
`;

export default SortOfferBarStyled;